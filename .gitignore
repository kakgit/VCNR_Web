node_modules/
.DS_Store
Thumbs.db
.gitrepo/
.venv/
__pycache__/
*.pyc
*.pyo
*.log
.env

backend-start.err
backend-start.log

Videos/
sample-videos/
mobile-viewer-app/
VCNR_Android_Player/
VCNR_Swarm_Test/

# Runtime media (uploaded posters, trailers, content chunks, torrents, manifests).
# Kept out of git — these are generated at runtime and are ephemeral on Render's
# free tier disk. media/.gitkeep is still tracked so the folder exists in the image.
media/library/
media/star_cast/
media/*.vcnr
media/*.torrent

# Local test screenshots / UI dumps (not part of the app).
*.png
*.xml
cloudflared-tunnel.err
